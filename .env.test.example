# Скопируйте в .env.test (файл в .gitignore) и поднимите БД: npm run test:db:up && npm run test:db:migrate
# Vitest подставляет TEST_DATABASE_URL вместо DATABASE_URL на время npm test (см. vitest.config.ts).

TEST_DATABASE_URL="postgresql://done48:done48@127.0.0.1:5433/done48_test?schema=public"
